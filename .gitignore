**/bin/
**/obj/
**/publish/

# Visual Studio per-solution cache — regenerable, and reached 1.3 GB locally
.vs/
data/*
weaverconfig.json

.formatter/node_modules/
.formatter/package-lock.json

# Test output artifacts
**/TestResults/
*.trx
**/TestResultsCoverage/
*.cobertura.xml

# Integration test scratch project directories (fake orchestrator runs write real
# files/commands here; should default to the OS temp dir, but ignore any repo-local
# fallback used for debugging a failed run)
**/.scratch/
tests/**/.fixtures/
